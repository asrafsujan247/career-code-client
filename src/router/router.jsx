import { createBrowserRouter } from "react-router";

const router = createBrowserRouter([
  {
    path: "/",
    element: <div className="text-3xl text-white">Hello World</div>,
  },
]);

export default router;
